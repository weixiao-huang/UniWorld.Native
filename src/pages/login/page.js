import React, { Component, PropTypes } from 'react'

import {
  MainView,
  MainText,
} from './style'

export default class Login extends Component {
  render() {
    return (
      <MainView>
        <MainText>This is Login Page</MainText>
      </MainView>
    )
  }
}
