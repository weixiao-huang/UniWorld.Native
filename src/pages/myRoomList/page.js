import React, { Component, PropTypes } from 'react'

import {
  MainView,
  TabView,
  StyledScrollTabView,
  MainText,
} from './style'

export default class MyRoomList extends Component {
  render() {
    return (
      <MainView>
        <TabView>

        </TabView>
      </MainView>
    )
  }
}
